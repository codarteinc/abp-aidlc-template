# Knowledge

This directory is populated by `/ai-dlc:elaborate` on the first deep-elaborate
run for an intent. Until then, treat it as empty.

Reference layouts (mirrors what LinkHub maintains):

- `architecture.md` — system topology, surfaces, key conventions.
- `conventions.md` — code style, naming, repo hygiene.
- `domain.md` — problem domain + entities.
- `product.md` — product principles + UX patterns.
- `design.md` — design tokens / system (omitted in a no-UI repo).
