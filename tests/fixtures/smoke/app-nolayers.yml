# tests/fixtures/smoke/app-nolayers.yml — schema-valid config for the
# single-layer template variant: app-nolayers + angular + ef + postgresql.

project_name: SmokeNolayers
github_owner: codarteinc-test

abp:
  template: app-nolayers
  ui: angular
  db_provider: ef
  dbms: postgresql
  tiered: false
  multi_tenancy: false
  default_culture: en
  optional_modules: []

infra:
  hetzner_location: hel1
  hetzner_server_type: cx23
  cloudflare_zone: REPLACE_ME
