# tests/fixtures/smoke/linkhub-equivalent.yml — schema-valid config
# matching the LinkHub-equivalent combo: app + angular + ef + postgresql.

project_name: SmokeApp
github_owner: codarteinc-test

abp:
  template: app
  ui: angular
  db_provider: ef
  dbms: postgresql
  tiered: false
  multi_tenancy: false
  default_culture: en
  optional_modules: []

infra:
  hetzner_location: hel1
  hetzner_server_type: cx23
  cloudflare_zone: REPLACE_ME
